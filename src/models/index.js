const userSchema = require('./user.model');
const refreshTokenSchema = require('./refreshToken.model');
const projectSchema = require('./project.model');

module.exports = {
    userSchema,
    refreshTokenSchema,
    projectSchema
}